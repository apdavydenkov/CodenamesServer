# TODO: Оставшиеся задачи

## ✅ Реализовано:

### Авторизация:
- ✅ authService.js с PIN-кодами (формат: ABCD-1234-EFGH)
- ✅ API эндпоинты: check-user, register, verify-pin, get-user, change-name, change-pin
- ✅ AuthDialog.jsx с многошаговой авторизацией
- ✅ Интеграция с чатом - авторизация перед входом
- ✅ Кнопка "Выйти" в чате
- ✅ userId в сообщениях
- ✅ Копирование PIN по клику

### Чат:
- ✅ Две вкладки: "Чат игры" и "Глобальный"
- ✅ Socket.IO комнаты работают корректно
- ✅ Свои сообщения справа (синие), чужие слева (белые)
- ✅ Кеширование сообщений при переключении вкладок
- ✅ Автообновление в реальном времени
- ✅ chatService.updateUsernameInMessages() для смены имени

---

## 📋 Что осталось сделать:

### 1. ✅ Счётчики непрочитанных сообщений (РЕАЛИЗОВАНО)

**Реализовано:**
- ✅ Состояние `unreadCounts` в App.jsx
- ✅ Отслеживание NEW_MESSAGE через Socket.IO
- ✅ Игнорирование своих сообщений
- ✅ Обнуление при открытии чата и переключении вкладок
- ✅ Бейдж на иконке чата в GameStatus
- ✅ Бейджи на вкладках чата

---

### 2. ✅ Заменить иконку чата на иконку пользователя (РЕАЛИЗОВАНО)

**Реализовано:**
- ✅ Заменён FiMessageSquare на FiUser в GameStatus.jsx
- ✅ Добавлен класс chat-button для позиционирования бейджа

---

### 3. UI для смены имени (опционально)

**Где добавить:** В меню профиля или в настройках чата

**API уже есть:**
```javascript
const response = await fetch('/api/auth/change-name', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({ userId, newUsername })
});
```

**Что происходит:**
- Проверяется, не занято ли новое имя
- Обновляется username в users.json
- Вызывается chatService.updateUsernameInMessages() - обновляет ВСЕ сообщения пользователя во всех чатах

---

## 🎯 Приоритеты:

1. ✅ **Счётчики непрочитанных** (ЗАВЕРШЕНО)
2. ✅ **Замена иконки** (ЗАВЕРШЕНО)
3. **UI для смены имени** (опционально, функционал уже работает через API)

---

## 🔑 Важные детали:

- **userId формат**: `user_${timestamp}_${random}`
- **PIN формат**: `ABCD-1234-EFGH` (2,088 триллионов комбинаций)
- **Сообщения**: `{ id, userId, author, text, timestamp }`
- **Комнаты Socket.IO**: `chat_${gameKey}` и `chat_GLOBAL_CHAT`
- **Кеш работает**: При переключении вкладок нет мерцания
